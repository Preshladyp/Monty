#include "monty.h"
#define _GNU_SOURCE
/**
 * f_stack -This should prints the top
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
